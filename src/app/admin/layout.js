export const metadata = {
  title: 'Good Times — Partner Admin',
  description: 'Internal admin dashboard for reviewing and managing partner applications.',
  robots: 'noindex, nofollow',
};

export default function AdminLayout({ children }) {
  return children;
}
